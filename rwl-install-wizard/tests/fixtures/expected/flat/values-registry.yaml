# values-registry.yaml (flat) — generated by rwl-install wizard. DO NOT hand-edit.
x-airgap-pinned-tags-notice:
  warning: PINNED subchart image tags in this overlay target chart ~0.2.x. VERIFY
    each against your Chart.lock and the FLAT air-gap image manifest in your USER-GUIDE
    before installing — the registry must hold the exact tag your chart resolves,
    or the image pull fails.
  pinnedTags:
    neo4j: 5.26.28-ubi10
    vault: 2.0.3
    bciBaseHelmTest: '15.7'
registryOverride: "flatreg.example/rw-virtual"
neo4j:
  disableLookups: true
  image:
    customImage: "flatreg.example/rw-virtual/library/neo4j:5.26.28-ubi10"
vault:
  server:
    image:
      repository: "flatreg.example/rw-virtual/hashicorp/vault"
      tag: 2.0.3
redis:
  image:
    registry: "flatreg.example/rw-virtual"
    repository: bitnamilegacy/redis
qdrant:
  disableLookups: true
  image:
    repository: "flatreg.example/rw-virtual/qdrant/qdrant"
  chartTests:
    dbInteraction:
      image: "flatreg.example/rw-virtual/bci/bci-base:15.7"
metricstore:
  image:
    registry: "flatreg.example/rw-virtual"
seaweedfs:
  global:
    seaweedfs:
      image:
        name: "flatreg.example/rw-virtual/chrislusf/seaweedfs"
  image:
    repository: "flatreg.example/rw-virtual/chrislusf/seaweedfs"
